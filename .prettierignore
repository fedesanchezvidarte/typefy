# Package Managers
package-lock.json
pnpm-lock.yaml
yarn.lock
bun.lock
bun.lockb

# Miscellaneous
/static/

# Paraglide (generated)
/src/lib/paraglide/
/project.inlang/

# Playwright output
test-results
playwright-report

# Supabase local-only state (machine-specific; ignored by supabase/.gitignore too)
/supabase/.temp/

# Vendored agent skills and docs tooling
.claude/
# Project prose docs (not code)
docs/
CONTEXT.md
CLAUDE.md

# Ingestion reports (generated by scripts/ingest.ts, committed as-is)
# Prettier pads their markdown tables; the generator emits them unpadded, so every
# ingest run would otherwise leave whitespace-only churn in the working tree.
/scripts/catalog/reports/
